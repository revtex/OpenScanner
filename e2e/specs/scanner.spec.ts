// Playwright spec — scanner UI interactions
import { test, expect } from '@playwright/test'

test.describe('Scanner UI', () => {
  test.todo('scanner page loads and LED panel is visible')
  test.todo('LIVE FEED button toggles live state')
  test.todo('SELECT TG panel opens and talkgroup toggles work')
  test.todo('TG selection persists across page reload')
  test.todo('SEARCH panel opens, filters apply, results render')
  test.todo('AVOID mutes talkgroup for 30 minutes')
})
