// Playwright spec — scanner UI interactions
import { test, expect } from '@playwright/test'

test.describe('Scanner UI', () => {
  test.fixme('scanner page loads and LED panel is visible', async () => {})
  test.fixme('LIVE FEED button toggles live state', async () => {})
  test.fixme('SELECT TG panel opens and talkgroup toggles work', async () => {})
  test.fixme('TG selection persists across page reload', async () => {})
  test.fixme('SEARCH panel opens, filters apply, results render', async () => {})
  test.fixme('AVOID mutes talkgroup for 30 minutes', async () => {})
})
