package-import-method=copy
# Keep the virtual store off the workspace mount (9p does not support the
# rename/link calls pnpm needs) but persistent across /tmp wipes.
# /home/vscode is on the container overlay and survives restarts.
virtual-store-dir=/home/vscode/.cache/pnpm-vstore/openscanner-frontend
